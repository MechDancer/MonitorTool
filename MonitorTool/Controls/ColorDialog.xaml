<ContentDialog
    x:Class="MonitorTool.Controls.ColorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MonitorTool.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="选择颜色"
    PrimaryButtonText="完成"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    DefaultButton="Primary">

    <ContentDialog.Resources>
        <DataTemplate x:Key="ColorTemplate" x:DataType="local:ColorItem">
            <StackPanel Orientation="Horizontal">
                <Button Background="{x:Bind local:ColorItem.ToBrush(Color), Mode=OneWay}"
                        Width="32" Height="32" Margin="12,4">
                    <Button.Flyout>
                        <Flyout>
                            <ColorPicker Color="{x:Bind Color, Mode=TwoWay}" 
                                         IsAlphaEnabled="True"/>
                        </Flyout>
                    </Button.Flyout>
                </Button>
                <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center" />
            </StackPanel>
        </DataTemplate>
    </ContentDialog.Resources>

    <ListView Name="MainList" ItemTemplate="{StaticResource ColorTemplate}" />
</ContentDialog>