<UserControl
    x:Class="MonitorTool.Controls.GraphicView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:local="using:MonitorTool.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Unloaded="GraphicView_OnUnloaded">

    <Grid>
        <canvas:CanvasControl Name="Canvas" Draw="CanvasControl_OnDraw" />
        <ListView Margin="24"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Top">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ColorStruct">
                    <Grid>
                        <TextBlock Text="{x:Bind Path=Topic}" />
                        <ColorPicker Color="{x:Bind Path=Color}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>